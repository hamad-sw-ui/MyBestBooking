# Rapport de validation — T-172 (métadonnées localisées + noindex)

- **Date** : 2026-09-01
- **Tâche** : T-172 (niveau **S**)
- **Résultat** : **CORRIGÉ (VALIDÉ)**

## Périmètre livré

1. `/recherche` : `generateMetadata` branché sur `search.meta.*`
   (clés orphelines depuis T-162 → désormais câblées).
2. Wrappers serveur + `*-client.tsx` : `connexion`, `inscription`,
   `mot-de-passe-oublie`, `reinitialiser`, `activer-compte` (auth) et
   `mon-compte` (main). Titres localisés FR/EN.
3. `verifier-email` (serveur) : titre + noindex.
4. Pages privées : `mon-compte`, `mes-reservations`, `mes-favoris`,
   `messages`, `messages/[id]`, `avis/[id]`, `reservation` → titre +
   `robots {index:false, follow:false}` ; `dashboard/layout.tsx` → noindex
   hérité.
5. `settings.ts` : défaut `supportedLocales: ["fr","en"]`.
6. Dictionnaire **1416 clés FR=EN** (+10 `*.meta.* / auth.meta.*`).

## Preuves automatisées

| Tag | Commande | Résultat |
|---|---|---|
| 🔨 | `npx tsc --noEmit` | **0 erreur** |
| 🔨 | `npm run build` (prod) | **0 erreur** — toutes routes compilées (wrappers inclus) |
| 🧹 | `npx eslint src/` | 0 erreur (warnings `<img>` préexistants identiques à la baseline) |
| 🧪 | `npx vitest run` | **432/432 passed, 0 échec, 0 skip** (suite complète, DB embarquée active — dont tests de routes DB) |
| 🔍 | `npm run i18n:check` | **0 candidat** |
| ▶️ | `npm run smoke` | **94/94 assertions** (dont réservation `POST /api/bookings` → 201 confirmée) |

## Preuves runtime (Next dev :3000, DB seedée)

```
FR : /recherche « Recherche d'hébergements | MyBestBooking » (indexable)
     /connexion « Connexion | MyBestBooking » (indexable)
     /inscription « Créer un compte | MyBestBooking » (indexable)
     /mot-de-passe-oublie « Mot de passe oublié | … » (noindex ✓)
EN : /recherche « Search accommodations | MyBestBooking »
     /connexion « Sign in | MyBestBooking »
Privé : /mon-compte /mes-reservations /mes-favoris /messages /reservation
        → titres localisés + <meta robots noindex> ×5 ✓
/dashboard → noindex ✓ · / (accueil) → titre générique inchangé, indexable ✓
GET /api/app-preferences → supportedLocales ["fr","en"] ✓
```

## Non-régression

- Contenu visible des pages : identique (seules les métadonnées `<head>`
  changent).
- Composants client déplacés à l'identique ; aucun import externe de route.
- Comportement FR par défaut et chaîne i18n (priorité compte > cookie >
  plateforme) inchangés — smoke complet le prouve de bout en bout.
- Baseline pré-T-172 : vitest 432 tests disponibles, smoke 94 — identiques
  après (delta = uniquement les +2 tests ajoutés session i18n précédente
  déjà comptés).

## Liens

- Audit : `REPORTS/audit_execution_2026-09-01_T172_uit.md`
- Impact : `REPORTS/analyse_impact_2026-09-01_T172_metadata_noindex.md`
- Conception : `REPORTS/analyse_conception_2026-09-01_T172_metadata_noindex.md`
