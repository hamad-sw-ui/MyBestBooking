# Rapport de validation — T-186 (visuels locaux + optimizer images)

- **Date** : 2026-09-02
- **Tâche** : T-186 (niveau **S**) — exécutée en 2 tours (quota 10 images/tour)
- **Résultat** : **CORRIGÉ (VALIDÉ)**

## Livré

1. **`src/lib/seed-images.ts`** (+3 tests) : `seedImageUrl(clé, legacy)` —
   chemin local si `public/seed-images/<clé>.jpg` existe, sinon URL
   Unsplash historique. Rollout piloté par les fichiers : zéro 404
   pendant la transition. 3 alias documentés (dest-tunis / hero-home =
   dar-el-medina-1 — fidélité historique — ; placeholder = villa-azure-1).
2. **`public/seed-images/`** : **23 visuels JPG locaux** (~7,2 Mo) —
   20 générés (8 propriétés ×2 + 4 destinations) + 3 alias.
3. **Seed / page / hero / destinations / fallbacks** migrés vers le
   résolveur ou le placeholder local ; fallback galerie legacy bidon
   (`…125${i}…`) remplacé par le placeholder (cosmétique).
4. **Propriétés existantes mises à jour par UPDATE ciblé** (le seed est
   idempotent) : les 8 biens servent des images 100 % locales.
5. **Optimizer `/_next/image` ACTIVÉ** (`unoptimized` retiré) — mesure
   runtime : `hotel-le-magnifique-1.jpg` **350 Ko → 49 Ko (−86 %)** et
   toutes les sources `<Image>` du HTML servi sont locales.
6. `next.config.ts` : remotePatterns Unsplash conservés (compat.

## Preuves

| Tag | Gate / sonde | Résultat |
|---|---|---|
| T1 | `vitest run` complet | **479/479** (72 fichiers, +3) |
| T2 | `tsc --noEmit` / eslint fichiers touchés | 0 erreur |
| T3 | `next build` | 60/60 ✅ |
| S1 | `npm run smoke` (SEED_TOKEN sourcé) | **94/94 ✅** |
| I1 | `i18n:check` | 0 écart ✅ |
| R1 | `/seed-images/*.jpg` statiques | 200 · `image/jpeg` ✅ |
| R2 | HTML `/` : toutes les sources `<Image>` = `/_next/image?url=%2Fseed-images…` | ✅ |
| R3 | Optimizer : 200, 49 159 o vs 350 504 o (originale) | **−86 %** ✅ |
| R4 | Base : 8/8 propriétés en `/seed-images/*` | ✅ |
| P1 | `npm run perf` | inchangé (p50 pages ≤ 17 ms, API 4 ms) ✅ |

## Régressions déclarées

Aucune. Rollout sans 404 (fallback legacy tant que fichier absent),
comportement historique préservé pour les galeries `<img>` natives.

## Note d'environnement

Le quota de génération (10 images/tour) a imposé 2 tours ; les 3 derniers
visuels (dest-tunis, hero, placeholder) sont des **alias locaux
documentés** de visuels générés — les fichiers dédiés pourront les
remplacer sans changement de code (présence détectée dynamiquement).
