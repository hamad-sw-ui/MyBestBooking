# Validation T-125 — Correctifs de l'audit fonctionnel n°5 (P1–P4)

- **Date** : 2026-08-28
- **Tâche** : T-125 (niveau S)
- **Origine** : `REPORTS/audit_fonctionnel_profond5_2026-08-28.md`
- **Analyse d'impact / conception** : `REPORTS/impact_T-125_2026-08-28.md`
- **Principe** : aucun comportement sain ne régresse ; tout changement visible est
  dérivé d'un réglage ou d'un code existant, avec **valeur par défaut qui
  conserve le comportement actuel**.

## Correctifs livrés

| Ref | Correctif | Fichiers clés |
|-----|-----------|----------------|
| **P1** | Modération des avis pilotée par réglage admin `reviews.requireModeration` (défaut `false` = publication immédiate historique ; `true` → avis `pending` dans la file `/dashboard/reviews`). Un auteur voit ses propres avis non approuvés (pas de fuite : condition sur son `userId`). | `src/lib/settings.ts`, `src/app/api/reviews/route.ts`, `src/components/reviews/review-form.tsx` |
| **P2** | Bouclage du parrainage : `users.referred_by` + `users.referral_rewarded_at` (migration `0017`), code généré à l'inscription, champ `referralCode` au register (non bloquant), champ + `?ref=` au formulaire, récompense idempotente versée au séjour terminé (cron) : parrain +10 €, filleul +5 € (réglables, activables/désactivables). | `src/lib/referral.ts` (nouveau), `src/db/schema.ts`, `drizzle/0017_referral-loop.sql`, `src/app/api/auth/register/route.ts`, `src/app/(auth)/inscription/page.tsx`, `src/app/api/cron/price-alerts/route.ts`, `src/components/referral-card.tsx` |
| **P3** | Le motif de suspension (`reason`) est journalisé dans l'audit `user.suspend` / `user.reactivate`. | `src/app/api/users/[id]/suspend/route.ts` |
| **P4** | La page de dépôt d'avis devient un Server Component avec garde : connecté requis, UUID valide, réservation existante et appartenant à l'utilisateur, séjour terminé → sinon `notFound()`. Le formulaire client est extrait dans `<ReviewForm/>`. | `src/app/(main)/mes-reservations/avis/[id]/page.tsx`, `src/components/reviews/review-form.tsx` |

## Preuves de validation (§13)

- 🔨 **typecheck** : `tsc --noEmit` **0 erreur**.
- 🔨 **lint** : **0 erreur** (15 warnings préexistants, aucun ajouté).
- 🔨 **build** : `✓ Compiled successfully` (build production complet, toutes routes générées).
- 🧪 **tests unitaires** : **245/245** sur 38 fichiers (5 nouveaux tests `src/lib/referral.test.ts` : génération/normalisation de code, calcul de récompense, programme désactivé, montants non négatifs ; test settings mis à jour pour la clé `reviews` et le sous-objet `referral`).
- 🧭 **ai:check** : **20 OK · 0 warn · 0 fail**.
- ▶️ **smoke HTTP** : **94/94 assertions**.

## Preuves d'exécution réelle (E2E, 3 rôles) — ▶️

### P2 — Parrainage (bouclage complet)
- Code du parrain (customer seed) = `4WHABQ4M`.
- Inscription filleul avec `referralCode` (espaces + casse mélangés) → **200** ; en base `referred_by` = id du parrain ✔, `referral_code` généré pour le filleul (`V8MVX2K5`) ✔.
- Inscription avec code **invalide** `ZZZZZZZZ` → **200** (non bloquant), `referred_by = null`, code propre généré ✔.
- Inscription **host** avec code → 200, `role=host`, parrain lié, code généré ✔.
- Séjour passé simulé (réservation `confirmed`+`paid`+checkOut passé) puis `GET /api/cron/price-alerts` → `completedBookings: 1` ;
  - wallet **parrain** : 25.00 → **35.00** (+10 €) ✔ ;
  - wallet **filleul** : 0.00 → **5.00** (+5 €) ✔ ;
  - `referral_rewarded_at` marqué ✔.
- **Idempotence** : 2nd run cron → `completedBookings: 0` ; wallets inchangés (35 / 5) ✔.

### P1 — Modération pilotée
- Défaut : `reviews.requireModeration = false` ✔ (préservé).
- `PATCH /api/admin/settings/reviews {requireModeration:true}` → 200, relu `true` ✔.
- Dépôt d'avis (séjour terminé) → statut **`pending`** ✔ ;
  - invisible publiquement (anonyme) ✔ ;
  - visible par son **auteur** connecté ✔ (aucune fuite : seulement ses propres avis) ;
  - présent dans la **file admin `status=pending`** ✔.
- `PATCH /api/reviews/[id]/moderate {status:approved}` → 200 → avis visible publiquement (4 avis publics) ✔.
- Remise à `false` → nouvel avis → statut **`approved`** (publication immédiate historique préservée) ✔.

### P3 — Motif de suspension tracé
- `PATCH /api/users/[id]/suspend {suspended:true, reason:"Motif de test…"}` → 200.
- Entrée d'audit `user.suspend` : `metadata.reason = "Motif de test T-125 vérification audit"` + `targetEmail` ✔ (auparavant seul `targetEmail` était journalisé).

### P4 — Garde RSC de la page d'avis
- Anonyme → **307** vers connexion ✔.
- UUID invalide / UUID inconnu / réservation d'autrui / séjour futur → rendu de la page **not-found** (« Cette page… introuvable / Retour à l'accueil »), **pas** le formulaire ✔.
- Propre réservation terminée → rendu du **formulaire** (« Partager votre expérience / Note globale / Publier mon avis ») ✔.
- Note : en dev Turbopack **et** en `next start`, `notFound()` sert le corps not-found avec un statut HTTP 200 — comportement **préexistant et homogène** dans le projet (vérifié sur `/hebergement/<slug-inexistant>` qui fait pareil). La sécurité réelle reste portée par l'API (404/403/400) ; la garde améliore l'UX (plus de formulaire mort). Aucune régression introduite.

## Migration

- `drizzle/0017_referral-loop.sql` : additive, 2 colonnes nullable + 1 FK auto-référence `ON DELETE SET NULL`. Aucune perte de données. Journal drizzle corrigé (entrée 0016 `sessions-token-text` manuelle ajoutée, 0017 référencée).
- Appliquée sur la base de dev (`referred_by`, `referral_rewarded_at` présentes).

## Données de test

Toutes les données créées pendant la validation ont été supprimées (comptes `*.t125@test.com`, réservations `MBB-2026-REF01/02` + avis associés, sessions/tokens). Wallet du customer seed remis à **25.00**, 2FA désactivé, agrégat d'avis de la propriété seed recalculé (`total_reviews=3`, `average_rating=8.9`), réglage `reviews.requireModeration` remis à **`false`**.

## Honnêteté technique (§16)

- 🔨 compilé (typecheck/build) · 🧪 testé (245 tests + 5 nouveaux) · ▶️ exécuté (parcours E2E ci-dessus) · 🔍 observé (code) · 🧠 déduit (statut 200 de notFound = comportement Next préexistant).
