# T-126 — Rapport de validation (§13/§16) : correctifs audit n°6 (P1, P2, P3)

- **Date** : 2026-08-28 · **Tâche** : T-126 (niveau **L**) · **Branche** : `arena/01a042cf-mybestbooking`
- **Origine** : `REPORTS/audit_fonctionnel_profond6_2026-08-28.md` ; conception `REPORTS/analyse_impact_T-126_2026-08-28.md`.
- **Principe** : durcissements **additifs** de validation, aucun contrat cassé, aucune migration, aucun impact financier.

## Correctifs

| Ref | Correctif | Fichiers |
|-----|-----------|----------|
| P1 | Promo : pourcentage ≤ 100 (type `percentage`) et `validUntil > validFrom` refusés à 400 (POST + garde PATCH + garde formulaire). | `src/app/api/promotions/route.ts`, `src/app/api/promotions/[id]/route.ts`, `src/components/promotion-form.tsx` |
| P2 | Double vote « utile » → **409 Conflict** (vérif d'existence avant le rate-limit) ; le 429 reste réservé au spam (rate-limit assoupli 3/h). | `src/app/api/reviews/[id]/helpful/route.ts` |
| P3 | Upload : vérification des **magic bytes** (`sniffImageMime`), rejet 400 si le contenu n'est pas une vraie image, quel que soit le Content-Type déclaré. | `src/app/api/properties/upload/route.ts`, `src/lib/storage/sniff.ts` (+ test) |

## 🔨 Validation statique
- `npx tsc --noEmit` → **0 erreur**.
- `eslint` (fichiers touchés) → **0 erreur, 0 warning**.
- `npm run build` (Next 16.2.6 Turbopack) → **succès**.

## 🧪 Tests automatisés
- `npm test` (vitest) → **251/251** (39 fichiers), dont les **6 nouveaux tests** de `src/lib/storage/sniff.test.ts` :
  JPEG (`FF D8 FF`), PNG (`89 50 4E 47…`), GIF (`GIF89a`/`GIF87a`), WebP (`RIFF….WEBP`), rejet texte/PDF déguisé, entrée vide/trop courte → `null`.
- `npm run smoke` → **94/94 assertions**.

## ▶️ Preuves d'exécution réelle (dev server, sessions fraîches admin/host/customer)

**P1 — promotions**
- POST `percentage value:150` → **400** « Une remise en pourcentage doit être comprise entre 0 et 100 ».
- POST `validUntil < validFrom` → **400** « La date de fin doit être postérieure à la date de début ».
- POST promo valide `value:25` → **201** (non-régression).
- POST `fixed_amount value:500` → **201** (le plafond 100 ne s'applique PAS aux montants fixes, non-régression).
- PATCH `validUntil` dans le passé (antérieur au début) → **400** ; PATCH `validUntil` +60 j → **200**.

**P2 — vote utile**
- 1er POST `/api/reviews/<id>/helpful` (customer) → **200** `helpfulCount:1`.
- 2e POST immédiat (doublon) → **409** « Vous avez déjà marqué cet avis comme utile » (avant : 429).
- POST anonyme → **401**.

**P3 — upload**
- Faux `.jpg` (texte, `Content-Type: image/jpeg`) → **400** « Le fichier n'est pas une image valide… ».
- PDF déguisé en `.png` → **400**.
- Vrai JPEG (`FF D8 FF…`) → **200** `mimeType:image/jpeg` (non-régression).
- Vrai PNG (`89 50 4E 47…`) → **200** `mimeType:image/png` (non-régression).
- POST customer → **403** (rôle host/admin uniquement, non-régression).

## 🎯 Non-régression
- Calcul financier **inchangé** : `applyPromoToTotal` reste défensif (`Math.min(discount, total)`) ; commission/wallet non touchés.
- Autorisations intactes : promo admin-only (403 autre rôle), vote connecté (401 anonyme), upload host/admin (403 customer).
- Les vraies images et les promos/fixed_amount légitimes passent toujours.

## 🧹 Nettoyage
- Promos de test `T126_VALID`, `T126_FIXED` supprimées (les 400 n'ont rien créé).
- Vote de test supprimé + `helpful_count` recalculé (0).
- Fichiers images de test supprimés de `public/uploads/`.
- Réservation smoke `MBB-2026-MBABXQ` supprimée.
- Aucune donnée résiduelle en base.
