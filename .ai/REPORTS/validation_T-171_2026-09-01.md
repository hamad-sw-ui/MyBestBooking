# Validation T-171 — e-mails transactionnels FR/EN

**Date** : 2026-09-01  
**Statut** : CORRIGÉ

## Delta vs T-170

T-170 fermait UI + facture + JSON API. Restait le trou visiteur EN : sujets/corps des gabarits `emailTemplates` encore en français dès que l’admin n’avait pas customisé (DEFAULTS FR).

## Périmètre livré

1. Copies FR/EN des 8 gabarits éditables (hors `newMessage` déjà T-150) dans `mail/strings.ts`.
2. `pickEditable` : DEFAULTS FR non custom → copie localisée (langue du destinataire) ; rédaction admin → telle quelle.
3. Tests : FR = DEFAULTS ; EN sujet/corps ; custom non auto-traduite.

Hors scope : arabe/RTL (repli FR) ; custom admin auto-traduit ; `"Invalid signature"` Stripe (webhook machine) ; UGC/seed ; libs métier FR.

## Preuves

| Tag | Commande | Résultat |
|---|---|---|
| 🔨 | `npx tsc --noEmit` | 0 erreur |
| 🔍 | `node scripts/check-i18n.mjs` | 0 candidat |
| 🧪 | `npx vitest run src/lib` | 327 passed / 12 skipped |
| ❓ | smoke HTTP | non rejoué |

## Non-régression

- Destinataire sans langue / `ar` → français (DEFAULTS).
- `makeT` inchangé ; `frenchZodMessage` FR ; API défaut **fr**.
