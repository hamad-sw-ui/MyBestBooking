# Validation T-169 — erreurs API interpolées, pays facture, 503 maintenance

**Date** : 2026-09-01  
**Statut** : CORRIGÉ

## Delta vs T-168

T-168 mappait uniquement les libellés **exacts**. Restait : templates (`Code promo : …`, MIME, stock, capacité, min-stay, paramètre, champ, test, bulk), 409 invité, mot de passe, sniff upload, 503 maintenance, ISO pays sur facture.

## Périmètre livré

1. **`localizeApiMessage`** (`src/lib/api-error.ts`) : dictionnaire exact étendu + préfixes récursifs `Code promo : ` → `Promo code: `, `Wallet : ` → `Wallet: ` + regex interpolées. Message inconnu inchangé.
2. **HTTP** : wrap `apiError` sur guest 409, change-password, properties param, uploads/avatar/properties MIME+taille, sniff uploads, availability stock, admin providers champ/test, bulk catch, `promotions/apply` `isPromoUsable`.
3. **Facture** : `propertyCountry` via `countryLabel` (FR « Maroc », EN « Morocco » ; repli ISO).
4. **`maintenanceResponse`** async → `apiError("Service momentanément en maintenance")`. Callers inchangés (handlers déjà async).

Hors scope : source FR des libs métier ; `"Invalid signature"` Stripe ; `makeT` ; e-mails éditables admin.

## Preuves

| Tag | Commande | Résultat |
|---|---|---|
| 🔨 | `npx tsc --noEmit` | 0 erreur |
| 🔍 | `node scripts/check-i18n.mjs` | 0 candidat |
| 🧪 | `npx vitest run src/lib` | 324 passed / 12 skipped |
| 🧪 | `api-error.test.ts` | défaut FR, exact EN, préfixes/regex |
| 🧪 | `invoice.test.ts` | Maroc (fr) / Morocco (en) |
| ❓ | smoke HTTP | non rejoué (pas de preview serveur) |

## Non-régression

- Cookie / préférence absents → messages **français**.
- `frenchZodMessage` reste FR.
- Tests `promotions.test.ts` conservent `{ error: "Réservation minimum 100.00" }` (lib FR).
