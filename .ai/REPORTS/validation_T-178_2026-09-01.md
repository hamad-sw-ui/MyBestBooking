# Rapport de validation — T-178 (lenteur de la preview)

- **Date** : 2026-09-01
- **Tâche** : T-178 (niveau **S**)
- **Résultat** : **CORRIGÉ (VALIDÉ)**

## Livré

1. Preview Arena servie en **production** (`next start`, build pré-généré
   60/60) au lieu de `next dev` (Turbopack compile à la demande).
2. **Mesures** : pages 10–24 ms (prod) contre 90–200 ms à chaud / jusqu'à
   2,2 s à froid (dev) ; gain ×3–×8.
3. Garde paiements prod : opt-in démo explicite `ALLOW_MOCK_PAYMENTS=true`
   (défaut : refus inchangé, testé `false`/absent).
4. Smoke compatible seed protégé (`x-seed-token` transmis si défini).

## Preuves

| Tag | Commande / sonde | Résultat |
|---|---|---|
| 📏 | curl timings dev vs prod (8 routes ×2 hits) | dev froid jusqu'à 2,2 s → prod **10–24 ms** stables |
| 🧪 | `vitest payment/index.test.ts` | **20/20** (+2 tests opt-in mock) |
| 🧪 | `npx vitest run` complet | **68 fichiers / 458 tests · 0 échec** |
| 🔨 | `tsc --noEmit` · `npm run build` | **0 erreur** (60/60) |
| ▶️ | `npm run smoke` **sur la prod :3000** | **94/94** (seed token + réservation mock OK) |
| ▶️ | pages live : `/`, `/recherche`, fiche, connexion | 200 · 10–50 ms |
| 🔍 | `i18n:check` · `eslint` | propres |
| ✅ | `ai:check` | **20 OK · 0 warn · 0 fail** |

## Note d'exploitation

- Les gardes de sécurité **réelles** (seed hors preview, Stripe en vraie
  prod) sont intactes : les dérogations sont confinées à `.env.local` de la
  preview.
- Pour retrouver le mode dev (hot-reload) lors d'une session codage :
  `npm run dev` ; la preview restera alors plus lente — attendu.
- Rebuild nécessaire après chaque modification de code en mode prod
  (`npm run build` puis relancer `next start`).

## Liens

- `REPORTS/audit_execution_2026-09-01_T178_lenteur.md`
