# Validation — T-107 : orchestration paiement et parcours complets

- **Date** : 2026-08-23
- **Statut** : VALIDÉ (environnement local PostgreSQL + Next dev)
- **Branche** : `arena/01a02dbb-mybestbooking`

## Chaîne obligatoire

| Commande / preuve | Résultat |
|---|---|
| `npx drizzle-kit migrate` sur base embarquée fraîche | ✅ migrations `0000` à `0013` ; colonnes `benefits_released_at`/`provider_message_id`, FK votes `ON DELETE CASCADE` contrôlées via `pg` |
| `npm run typecheck` | ✅ succès |
| `npm run lint` | ✅ 0 erreur ; 16 warnings historiques non bloquants (`<img>`, directives existantes, dépendance promotions) |
| `npm test` | ✅ **33 fichiers, 218/218 tests** ; inclut keyring DB, mailer idempotent et headers Stripe |
| `npm run build` | ✅ Next production ; route `/api/admin/providers/rotation` compilée |
| `npm run smoke` | ✅ **91/91** assertions HTTP/RBAC/pages/parcours booking |

## Scénarios runtime ciblés

| Scénario | Résultat observable |
|---|---|
| booking mock après refactor | `POST /api/bookings` → `201`, `status=confirmed`, `paymentStatus=paid`, `paymentIntentId=pi_mock_*` |
| paiement `succeeded` tardif après expiration | webhook mock → `200 {processed:1}` ; DB : `status=cancelled`, `payment_status=paid`, `refund_status=refunded`, `refund_provider_id` présent |
| retry événement tardif | second événement différent → traité sans passage `confirmed` ni second état financier |
| retry outbox après lease expiré | `attempts=2`, `provider_message_id` inchangé, nombre de fichiers Console inchangé (2 → 2) |
| bulk admin avis avec vote | `POST /api/admin/bulk` → `200 succeeded:1`; DB : reviews=0, votes=0 |
| quote alerte contextualisé | dates 2026-12-10→12, 1 adulte, overrides 99 : cron `notified:1`, `last_notified_price=198.00` |
| pagination hors bornes | `/recherche?page=999` → `200`, HTML/RSC : 8 résultats, page 1 sur 1, pas de lien suivant vide |
| PATCH rate plan | création puis PATCH host → `200`, type/remise/petit-déjeuner/politique/jours retournés modifiés |
| key rotation | test DB : déchiffrement via clé précédente, réchiffrement primaire, lecture après retrait précédente ✅ |
| endpoint rotation non authentifié | `POST /api/admin/providers/rotation` → `403` |

## Limites de preuve

- Aucun compte ni secret test Stripe, Resend ou S3/R2 n’a été fourni : les appels fournisseurs réels ne sont pas déclarés validés. Les headers/idempotences et comportements mock ont été testés.
- Playwright navigateur/Chromium demeure indisponible dans ce sandbox (téléchargement bloqué). Les pages et APIs ont été vérifiées par build, smoke HTTP et scénarios runtime, mais aucune affirmation d’E2E navigateur n’est faite.

## Conclusion

Les critères C sont satisfaits : migration fraîche, compilation, lint sans erreur, suite automatisée, build, smoke et scénarios financiers/administratifs réels passent. BUG-034 peut être fermé, avec les limites explicites ci-dessus.
