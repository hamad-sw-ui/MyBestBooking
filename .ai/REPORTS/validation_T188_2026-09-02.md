# Rapport de validation — T-188 (SmartImage + cron preview actif)

- **Date** : 2026-09-02
- **Tâche** : T-188 (niveau **S**)
- **Résultat** : **CORRIGÉ (VALIDÉ)**

## Livré

1. **(a)** `SmartImage` + `isLocallyServedImage` (+5 tests) ; 11 `<img>`
   migrés, conteneurs `relative` posés ; `user-avatar` conservé natif par
   design (onError→initiales).
2. **(b)** Cron preview **rendu vivant** : `CRON_SECRET` en `.env.local`,
   `scripts/cron-runner.mjs` + `npm run cron:local`, process actif (tick
   horaire). Preuves handler : alerte notifiée (148,33), séjour passé
   clôturé (loyaltyAwardedAt, compteur 7→8), review request + alert mail
   `sent`, **idempotence** (2ᵉ tick = 0 effet).
3. **(c)** BestRewards checkout vérifié en réel : 945,00 → −176,72 →
   862,78, confirmed/paid.
4. Purge de toutes les fixtures (0 restant) ; compteur BestRewards
   restauré.
5. eslint : warnings `no-img-element` résolus sur les fichiers migrés
   (directives compactes corrigées ; 1 warning useEffect préexistant,
   hors périmètre).

## Preuves

| Tag | Gate / sonde | Résultat |
|---|---|---|
| T1 | `vitest run` | **484/484** (73 fichiers, +5) — isolé ✅ ×2 |
| T2 | `tsc --noEmit` | 0 erreur |
| T3 | eslint fichiers touchés | 0 erreur, warnings no-img résolus |
| T4 | `next build` | 60/60 ✅ |
| S1 | `npm run smoke` | **94/94 ✅** (rejoué après purges) |
| I1 | `i18n:check` | 0 écart ✅ |
| R1 | Fiche : galerie = `/_next/image` + srcset 256…3840w, hero `priority` | ✅ |
| R2 | Pages connectées : réservations/dashboards servent `_next/image` | ✅ (listes vides = 0, cohérent) |
| R3 | Cron sans Bearer → 401 conservé ; avec → 200 + sous-tâches > 0 ; 2ᵉ tick 0 | ✅ |
| R4 | Runner local : `CRON_ONCE=1` → HTTP 200 ; process Arena actif | ✅ |
| R5 | Cashback 0 € au niveau 2 conforme à `loyalty.ts` (5 % Ambassador) | ✅ |
| P1 | Base post-purge : booking/alerte/mails/compteur — état seed | ✅ |

## Régressions déclarées

Aucune. 401 sans secret maintenu, handler intact, images externes rendues
comme avant (lazy en plus). Règle opérationnelle ajoutée : ne jamais
enchaîner smoke→vitest sur la même base (interférence documentée T-187/188).
