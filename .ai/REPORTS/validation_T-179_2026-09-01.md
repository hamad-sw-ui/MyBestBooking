# Rapport de validation — T-179 (garde maintenance pour les pages, au proxy)

- **Date** : 2026-09-01
- **Tâche** : T-179 (niveau **S**)
- **Résultat** : **CORRIGÉ (VALIDÉ)**

## Livré

1. **`src/proxy.ts`** : mode maintenance au proxy — 307 `/maintenance` pour
   tout non-admin hors whitelist, avant tout rendu (couvre enfin `/` et
   toutes les pages).
2. Whitelist intacte (`/connexion`, `/inscription`, `/maintenance`,
   `/api/auth/*`, `/api/admin/*`, assets), anti-lockout admin inchangé.
3. Sonde en échec → passthrough **loggé** ; chaque détournement **tracé**
   (serveur : `[proxy] maintenance active — redirection …`).
4. **+7 tests** (`proxy-maintenance.test.ts`) : OFF inchangé, 307 racine et
   pages, protégée non-admin, whitelist connexion, admin traverse, token
   legacy sans rôle = prudent, sonde ko → passe. `proxy.test.ts` historique
   immunisé de l'état maintenance (mock).
5. Audit admin/hôte complet en prod : modération avis (pending→approuvé→
   masqué+agrégat recalculé), maintenance API 503, gardes RBAC — sains.

## Preuves

| Tag | Commande / sonde | Résultat |
|---|---|---|
| 🧪 | `vitest proxy-maintenance.test.ts` | **7/7** |
| 🧪 | `npx vitest run` complet | **69 fichiers / 465 tests · 0 échec** |
| 🔨 | `tsc --noEmit` · `npm run build` | **0 erreur** (60/60) |
| 🧹 | `eslint` fichiers touchés | **0 erreur** |
| ▶️ | PROD maint.ON : anon `/`, `/recherche` → **307 /maintenance** | ✅ |
| ▶️ | PROD maint.ON : client page protégée → 307 ; **admin traverse 200** ; `/connexion` 200 anti-lockout ; `/maintenance` 200 | ✅ |
| ▶️ | OFF + TTL 60 s → retour normal (`/recherche` 200) | ✅ propagation mesurée |
| ▶️ | `npm run smoke` sur la prod | **94/94** |
| 🔍 | `i18n:check` | **0 candidat** |

## Liens

- `REPORTS/audit_execution_2026-09-01_T179_maintenance_pages.md`
- `REPORTS/analyse_impact_2026-09-01_T179_maintenance_pages.md`
- `REPORTS/analyse_conception_2026-09-01_T179_maintenance_pages.md`
