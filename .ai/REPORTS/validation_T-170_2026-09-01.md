# Validation T-170 — succès API, bulk, transitions

**Date** : 2026-09-01  
**Statut** : CORRIGÉ

## Delta vs T-169

T-169 couvrait les `error` interpolés. Restait : JSON `message` (succès affichés UI : vérif e-mail, reset, login…), motifs `skipped[]`/`failed[]` bulk admin, transitions booking, Zod login/register/forgot encore en anglais par défaut.

## Périmètre livré

1. **Succès HTTP** wrappés `apiError` : login, register, forgot/reset/change-password, resend-verification, chambre supprimée, hébergement archivé, seed, `Connexion {provider} validée`.
2. **Bulk** : `localizeBulkResult` à la frontière HTTP (raisons skip + erreurs failed).
3. **Dict / regex** : transitions, skip bulk, provider 32 octets, `Email invalide`.
4. **Zod** : `frenchZodMessage` sur login, register, forgot-password.

Hors scope : source FR des libs métier ; `"Invalid signature"` Stripe ; corps d’e-mails éditables admin ; `makeT`.

## Preuves

| Tag | Commande | Résultat |
|---|---|---|
| 🔨 | `npx tsc --noEmit` | 0 erreur |
| 🔍 | `node scripts/check-i18n.mjs` | 0 candidat |
| 🧪 | `npx vitest run src/lib` | 324 passed / 12 skipped |
| 🧪 | `api-error.test.ts` | succès, transitions, skip bulk |
| ❓ | smoke HTTP | non rejoué |

## Non-régression

- Cookie / préférence absents → messages **français**.
- `frenchZodMessage` reste FR.
- Audit bulk conserve les motifs FR internes (localisation uniquement sur la réponse JSON).
