# Validation — T-263 (B10) et T-264 (B12)

- **Date** : 2026-09-11 · **Branche** : `arena/01a08b7d-mybestbooking`
- **Périmètre** : constats **B10** (résidus T-207 récapitulés, aucune suppression) et **B12**
  (rate-limit en mémoire énoncé au déploiement).
- **Analyse source** : `docs/analyse_2026-09-11_audit_runtime_inacheves_mal_penses.md` § 3.10, § 3.12

## 1. Livré

| Élément | État |
|---|---|
| Entrée « Dette T-207 (audit n°6, B10) » dans `KNOWN_LIMITATIONS.md` | ✅ liste les 5 résidus, leur raison d'être et leurs conditions de retrait ; signale les colonnes **à ne pas nettoyer** |
| Marqueurs `// legacy:` | ✅ `(main)/reservation/reservation-form.tsx` (lecture `propertyId`/`roomId`) et `dashboard/rooms/[id]/page.tsx` (redirection) |
| Ligne de checklist production rate-limit | ✅ `docs/CI.md` : « au-delà d'une instance, la limite effective est divisée par le nombre d'instances → prévoir Redis » |
| Avertissement unique au démarrage | ✅ `src/lib/rate-limit.ts` : une fois par processus, en production, seulement sans `REDIS_URL` (log, aucun impact runtime) |
| Aucune suppression de route / de colonne | ✅ `POST /api/bookings/[id]/payment` et `GET /api/cron/payouts` restent en **410 explicite** |
| i18n | 0 clé ajoutée (verrou 1749 inchangé) |

## 2. Preuves automatisées

- `src/lib/rate-limit.test.ts` **11/11**, dont le nouveau test « avertissement unique en production
  sans stockage partagé » (`vi.stubEnv`, deux appels → un seul log).
- `npx tsc --noEmit` 0 · `npx eslint src --max-warnings 0` 0/0.
- Suite complète relancée dans la foulée (voir commit) : les limites existantes restent intactes
  (les tests de rate-limit des routes de connexion/inscription passent inchangés).
- Vérification documentaire : `grep -n "legacy"` retrouve les deux marqueurs et l'entrée
  `KNOWN_LIMITATIONS.md` ; `docs/CI.md` porte la ligne de checklist.

## 3. Portée

Aucun changement de comportement observable : la dette est **rendue visible** (documentation,
marqueurs, log unique) au lieu d'être supprimée ou silencieuse. La décision produit (retirer les
résidus, migrer le limiteur) reste ouverte et documentée.
