# Validation T-168 — i18n 100 % (facture, placeholders, erreurs API)

**Date** : 2026-09-01  
**Statut** : CORRIGÉ (VALIDÉ)

## Périmètre livré

1. **Facture / reçu HTML** (`src/lib/invoice.ts`) : titres, barre d’impression, voyageur, dates, totaux, note de reçu, `html lang`, nom de fichier (`facture`/`invoice`, `recu`/`receipt`). Locale via `getServerLocale`. Défaut **fr**.
2. **Placeholders réglages facturation** (`settings-panel.tsx`) : 5 clés `settings.ph*`.
3. **Erreurs JSON API** : source française conservée ; `apiError(fr)` traduit si locale UI = `en`. `frenchZodMessage` reste FR (tests/smoke).

Hors 100 % volontaire : corps d’e-mails **éditables admin** (langue de l’admin) ; codes ISO/OTP/devises/marque ; `makeT` sans interpolation.

## Preuves

| Tag | Commande | Résultat |
|---|---|---|
| 🔨 | `npx tsc --noEmit` | 0 erreur |
| 🔍 | `node scripts/check-i18n.mjs` | 0 candidat |
| 🧪 | `npx vitest run src/lib` | 323 passed / 12 skipped |
| 🧪 | `ui-strings.test.ts` | 1394 clés FR=EN |
| 🧪 | `invoice.test.ts` | reçu FR + facture EN |
| 🧪 | `api-error.test.ts` | défaut FR, EN mappé |
| 🧪 | `http.test.ts` | `frenchZodMessage` FR inchangé |
| ❓ | smoke HTTP | non rejoué (pas de preview serveur) |

## Non-régression

- Cookie / préférence absents → messages **français**.
- `src/proxy.ts` matcher public non touché.
- T-154d BestRewards 15≠17 non traité (hors i18n).
