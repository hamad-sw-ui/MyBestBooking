# Rapport de validation — T-180 (entrée « devenir hôte » pilotée par le rôle)

- **Date** : 2026-09-02
- **Tâche** : T-180 (niveau **S**)
- **Résultat** : **CORRIGÉ (VALIDÉ)**

## Livré

1. **`src/lib/host-entry.ts`** : `hostEntryHref(role)` + `initialRoleFromSearchParam`, fonctions pures.
2. **`footer.tsx`** : prop optionnelle `userRole` ; lien « Ajouter mon hébergement » contextuel.
3. **`(main)/layout.tsx` + `page.tsx`** : passent `user?.role ?? null` au Footer.
4. **`register-client.tsx`** : présélection du rôle via `?role=host`.
5. **+5 tests** (`host-entry.test.ts`) — audit d'exécution complet joint (12 zones saines, 1 défaut corrigé).
6. Effet de bord d'audit résorbé : mot de passe `customer@mybestbooking.com` restauré à `Customer123!` (poste de fumée conforme).

## Preuves

| Tag | Commande / sonde | Résultat |
|---|---|---|
| T1 | `npx vitest run src/lib/host-entry.test.ts` | **5/5 ✅** |
| T2 | `npx vitest run` (complet) | **70 fichiers / 470 tests ✅** (465+5) |
| T3 | `npx tsc --noEmit` | 0 erreur |
| T4 | `eslint` (fichiers touchés) | 0 erreur |
| T5 | `next build` | 60/60 routes ✅ |
| R1 | Probe `/` anonyme → href footer | `/inscription?role=host` ✅ |
| R2 | Probe `/` customer → href footer | `/inscription?role=host` ✅ |
| R3 | Probe `/` host → href footer | `/dashboard/properties/new` (inchangé) ✅ |
| R4 | Idem R1/R2/R3 sur `/recherche` (layout `(main)`) | identiques ✅ |
| R5 | Host : `GET /dashboard/properties/new` | **200** (non-régression) ✅ |
| R6 | `GET /inscription?role=host` | **200** ✅ |
| I1 | `npm run i18n:check` | 0 écart (aucune clé ajoutée) ✅ |
| S1 | `SEED_TOKEN` sourcé + `npm run smoke` | **94/94 ✅** |

## Régressions déclarées

Aucune. Hôte/admin à l'identique (T1, R3, R5) ; suite complète, build,
i18n et smoke verts ; les autres consommateurs du `Footer` restent valides
(prop optionnelle, tsc T3).

## Note d'environnement

Le smoke exige `SEED_TOKEN` dans l'environnement du shell en prod (garde
T-178) ; il suffit de sourcer `.env.local` avant (`set -a; . ./.env.local; set +a`).
