# T-127 — Rapport de validation (§13/§16) : correctifs audit n°7 (P1, P2, P3)

- **Date** : 2026-08-28 · **Niveau** : L · **Branche** : `arena/01a042cf-mybestbooking`.
- **Origine** : `REPORTS/audit_fonctionnel_profond7_2026-08-28.md` ; impact : `REPORTS/analyse_impact_T-127_2026-08-28.md`.

## Correctifs

- **P1** : existence de la propriété vérifiée avant insertion d'une alerte de prix (`price-alerts`) et d'un favori (`wishlists`) → **404** au lieu d'un 500 FK.
- **P2** : `sniffImageMime` (T-126) appliqué à l'upload des pièces jointes de messagerie (`/api/uploads`) ; rejet 400 d'un non-image, MIME réel stocké.
- **P3** : export facturation filtrable par `from`/`to` validés ; sans paramètre = export complet (historique).

## 🔨 Statique
- `tsc --noEmit` → **0 erreur**.
- `eslint` (4 fichiers touchés) → **0 erreur, 0 warning**.
- `next build` → **succès**.

## 🧪 Tests
- `vitest run` → **251/251** (39 fichiers).
- `npm run smoke` → **94/94 assertions**.

## ▶️ Preuves d'exécution réelle (sessions customer/host/admin fraîches)

**P1 — propriété inexistante**
- `POST /api/price-alerts {propertyId:"00000000-…"}` → **404** « Hébergement introuvable » (avant : 500).
- Alerte sur propriété existante `maxPrice:120` → **201** (non-régression).
- `POST /api/wishlists {wishlistId:<mienne>, propertyId:"00000000-…"}` → **404** « Hébergement introuvable » (avant : 500).
- Ajout d'une propriété existante → **201** (non-régression).

**P2 — pièce jointe**
- Faux `.jpg` (texte, `Content-Type: image/jpeg`) → **400** « Le fichier n'est pas une image valide… » (avant : 200).
- Vrai JPEG (`FF D8 FF…`) → **200**, `mimeType:"image/jpeg"` (non-régression).

**P3 — export de facturation**
- Sans paramètre → **200** (36 lignes admin).
- `from=2020-01-01&to=2030-01-01` → **200**, 36 lignes (= complet).
- `from=2001-01-01&to=2001-12-31` (période vide) → **200**, **0 ligne** de données (le filtre agit réellement).
- Période inversée (`from>to`) → **400** ; date mal formée (`31/12/2026`) → **400**.
- customer → **403** (non-régression).

## 🎯 Non-régression
- Aucun flux financier/réservation modifié. La vérification d'existence ne durcit pas sur `status='active'` (un favori sur une propriété suspendue n'est pas cassé).
- Autorisations intactes : alerte/favori/pièce jointe connectés (401 anonyme), export host/admin (403 customer).

## 🧹 Nettoyage
- Wishlist de test (« Audit7 fix ») + item supprimés ; alerte `maxPrice:120` supprimée ; fausse/vraie pièce jointe (`upload_objects` + fichier disque) supprimées ; réservation smoke `MBB-2026-5OLJOW` supprimée.
- Contrôle final : **0** alerte orpheline, **0** item orphelin en base.
