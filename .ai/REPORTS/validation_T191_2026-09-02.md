# Rapport de validation — T-191 (CI : gates reproductibles)

- **Date** : 2026-09-02
- **Tâche** : T-191 (niveau **S**)
- **Résultat** : **CORRIGÉ (VALIDÉ)**

## Livré

1. **`npm run ci`** (`scripts/ci.sh`) — chaîne reproductible : tsc →
   eslint `--max-warnings 0` (le zéro-warning T-189 devient un gate dur) →
   i18n → ai:check → vitest → build → smoke, **dans cet ordre imposé**
   (leçon T-187/188/189 : smoke→vitest = tests transitoires cassés ; les
   tests d'intégration supposent la base démo seedée).
2. **Workflow GitHub Actions prêt à l'activation** (`docs/ci-workflow.yml`)
   — le push de `.github/workflows/*` est refusé par GitHub (l'App du
   sandbox n'a pas la permission `workflows`). Le fichier est versionné
   sous `docs/` avec `docs/CI.md` ; l'activation est un simple déplacement.
   En CI distante, les bases sont strictement disjointes (service
   postgres:16 pour vitest, embedded pour le smoke).

## Preuves (exécutions réelles, pas des promesses)

| Run | Contenu | Résultat |
|---|---|---|
| tentative « base jetable vitest » | vitest sur `app_db_ci_<pid>` vierge | 62 fichiers OK / **11 KO** — « Seed non appliqué (customer introuvable) » → hypothèse abandonnée, ordre imposé à la place |
| tentative smoke sans sourcing | seed → 404 (`SEED_TOKEN` non exporté) | KO — corrigé (`set -a; . ./.env.local; set +a` dans ci.sh) |
| **final** | chaîne complète | **EXIT=0** : tsc 0 · eslint 0/0 · i18n 0 · ai:check 20/0/0 · vitest **484/484** · build ok · smoke **94/94** |

## Contrainte externe déclarée (non contournable dans ce cadre)

Run GitHub Actions impossible à observer : permission `workflows` absente
de l'App. La preuve mesurable retenue = `npm run ci` vert localement
(chaîne identique, joueur pour joueur). Régressions produit : **aucune**
(seuls `scripts/ci.sh`, `docs/*`, un script npm ajoutés).
