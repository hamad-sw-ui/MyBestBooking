# T-152 — Implémentation des findings de l'audit fonctionnel n°24 (A→E + G)

**Date :** 2026-08-30
**Branche :** `arena/01a052ed-mybestbooking` (base `99b4c7f` — T-151)
**Niveau de proportionnalité :** **S — Système**
**Justification :** implémentation de 5 findings + 1 observation sur un
périmètre transversal (tunnel de réservation/paiement, analytics/billing,
i18n UI, état des avis, smoke) : ~18 fichiers, 2 nouveaux composants, 2
nouveaux helpers, 13 nouveaux tests. Aucune migration DB, aucune signature
d'API retirée (champs/trames additives uniquement) ; niveau retenu selon la
règle « en cas de doute entre deux niveaux, choisir le plus élevé ».
**Statut :** CORRIGÉ (VALIDÉ) — 2026-08-30

---

## 0. Objet

Le rapport `REPORTS/audit_fonctionnel_profond24_2026-08-30.md` (T-151)
identifie 5 findings de cohérence (A→E) et 1 observation (G), tous
**additifs** ou remplacement local d'affichage. Cette tâche les implémente
dans l'ordre du tableau « Solutions retenues » du rapport, sans retrait de
contrat public, sans schéma modifié, avec défauts conservés (langue `fr`,
devise EUR).

Rapports préparatoires (déjà en place, exigés §14/§15.1 pour un niveau S) :
`analyse_impact_T-152_2026-08-30.md`, `analyse_conception_T-152_2026-08-30.md`,
`opportunites_T-152_2026-08-30.md`.

## 1. Modifications (par finding)

### A — Réservation `pending` actionnable (payer / annuler)

| Fichier | Changement |
|---|---|
| `src/components/booking-row-actions.tsx` | prop `paymentStatus?` ; `pending`+`paymentStatus=pending` → **« Payer maintenant »** (lien `/reservation?booking={id}`) ; `pending` sans attente → badge « Paiement en cours de confirmation » ; annulation étendue à `confirmed \|\| pending` |
| `src/app/(main)/reservation/page.tsx` | lecture `?booking=` ; chargement de la réservation (`GET /api/bookings/[id]`), gardes (déjà confirmée / non reprenable), pré-remplissage, puis **reprise automatique** de `POST /api/bookings/[id]/payment` (un seul essai, refs React mises à jour en `useEffect` uniquement) ; écran d'erreur → retour « Voir mes réservations » |
| `src/lib/ui-strings.ts` | clés FR/EN `reservation.resumeError`, `cannotResume`, `alreadyConfirmed`, `payPending`, `seeBookings` |

✅ Aucun changement d'API : `POST /api/bookings/[id]/payment` et l'annulation
`pending` existaient déjà (audit A = « API prête »).

### B — Devise réelle dans le tunnel de réservation

`src/app/(main)/reservation/page.tsx` : `RoomData.currency?` ajouté
(l'API renvoie déjà `rooms.currency`), `roomCurrency = room?.currency ?? "EUR"`,
**toutes** les occurrences `€{…}` (prix/nuit, sous-total, remise, taxes,
remise promo, total, confirmation) remplacées par `formatPrice(montant,
roomCurrency)`. Le wallet reste affiché en EUR (crédit produit libellé EUR —
hors périmètre audit) ; le solde à payer après wallet est converti dans la
devise de la chambre.

### C — Totaux analytics / billing par devise

| Fichier | Changement |
|---|---|
| `src/lib/currency-summary.ts` (nouveau) | `sumByCurrency` (ignore NaN/Infinity, normalise devise, défaut EUR), `topCurrency` (EUR prioritaire à égalité), `currenciesOf` (ordre stable), `hasMixedCurrencies`, `formatCurrencyBreakdown` (`100,00 € + 50,00 $US`, jamais de somme inter-devises) |
| `src/app/dashboard/analytics/page.tsx` | revenus courant/précédent, panier moyen et revenus par propriété agrégés **par devise** ; série « Revenus par jour » sur la devise dominante (`chartCurrency`), autres devises signalées par un bandeau ambre sans être mélangées |
| `src/app/dashboard/billing/page.tsx` | nets hôte (mois courant / mois précédent / total) par devise ; ligne récapitulative facture EUR explicite |

### D — Sélecteur de langue FR/EN + `<html lang>` dynamique

| Fichier | Changement |
|---|---|
| `src/components/language-selector.tsx` (nouveau) | `<select>` FR/EN (aucune nouvelle chaîne : `t("account.language")` existe déjà FR/EN) ; compte connecté → `PATCH /api/users/me {language}` puis `resetDisplayPreferencesCache()` + reload ; anonyme → localStorage `mybb:ui-language` + bascule immédiate ; erreur affichée sans reload |
| `src/components/layout/header.tsx` | `<LanguageSelector user={user} />` |
| `src/app/layout.tsx` | `<html lang={await getServerLocale()}>` + script `lang-init` (avant hydratation, uniquement si anonyme, borné à `en`/`fr`) |
| `src/lib/use-display-currency.ts` | export `UI_LANGUAGE_STORAGE_KEY` ; priorité **compte > localStorage > plateforme > fr** (la préférence locale ne prime jamais sur le profil) |
| `src/app/api/users/me/route.ts` | PATCH `language` déjà géré (aucune modification nécessaire — vérifié) |

### E — État de l'avis (plus de CTA « doublon »)

| Fichier | Changement |
|---|---|
| `src/app/api/bookings/route.ts` | `leftJoin(reviews)` + champ **additif** `review {id, overallRating, status} \| null` |
| `src/app/(main)/mes-reservations/page.tsx` | si `review.id` → badge vert « ✓ Avis publié (X/10) » / « En attente de modération » / « Avis soumis » ; sinon CTA actuel inchangé ; `paymentStatus` transmis à `BookingRowActions` |
| `src/app/(main)/mes-reservations/avis/[id]/page.tsx` | si avis existant → écran « Vous avez déjà publié un avis » + note `X/10` + liens (hébergement, retour) ; sinon `ReviewForm` inchangé |

### G — Smoke auto-suffisant (wishlist)

`scripts/smoke.sh` : si la wishlist seed est absente, `POST /api/wishlists`
(vide) → récupération de l'id (201/200) ; header `@assertions` réconcilié
**85 → 94**.

## 2. Preuves

| Vérification | Résultat |
|---|---|
| 🔨 `npm run typecheck` | **0 erreur** |
| 🔨 `npm run lint` | **0 erreur** — 14 warnings **préexistants** (liste de fichiers identique à la baseline `99b4c7f`, vérifiée par stash) |
| 🧪 `npm test` | **329/329** (47 fichiers) — 316 existants + **13 nouveaux** ; 0 skip (serveur dev :3000 actif → les 12 tests HTTP préexistants `admin/bulk`, T-034, s'exécutent au lieu d'être skippés) |
| ▶️ `npm run smoke` | **94/94 PASS, 0 FAIL** |
| 🔨 `npm run build` | **OK** (Next.js 16.2.6, 36 s) |
| 🔨 `npm run ai:check` | **19 OK · 1 warn · 0 fail** (warn = STATE.md « à mettre à jour en fin de session » ; résolu par ce commit) |
| ▶️ Preuves runtime HTTP | cf. §2.2 |

### 2.1 Nouveaux tests (13)

- `src/app/api/bookings/[id]/payment/route.test.ts` (**3**, intégration DB
  réelle, mocks PSP, skip si DB absente) :
  1. réservation `pending` reprise → **200** + paiement prêt à confirmer ;
  2. intent expiré / non reprenable → **409** (aucune fausse promesse) ;
  3. non-propriétaire → **403** avant tout appel PSP.
- `src/app/api/bookings/route.test.ts` (**2**, GET) : `review {id,status,
  overallRating}` présent pour une résa commentée ; `review: null` pour une
  résa sans avis (additif, aucun appelant cassé).
- `src/lib/currency-summary.test.ts` (**7**) : somme par devise sans mélange ;
  map vide ; `topCurrency` (EUR prioritaire à égalité) ; `currenciesOf` /
  `hasMixedCurrencies` (ordre stable) ; `formatCurrencyBreakdown` mono-devise
  = rendu historique EUR ; devises multiples → liste jointe jamais additionnée ;
  map vide → `0,00 €`.
- `src/lib/utils.test.ts` (**1**) : `formatPrice(100, "USD")` ≠ EUR
  (`$US` présent, pas de `€`).

### 2.2 Preuves runtime (dev :3000 + Postgres 55432)

- **A** — réservation `pending` (ref `MBB-2026-CMDMD9`,
  `827b17b6-5e58-4085-a403-50d0f90c6260`) : GET puis POST
  `/api/bookings/827b…/payment` → `status: confirmed`,
  `paymentStatus: paid`, `paymentIntentId: pi_mock_c454a923-…` (reprise
  effective d'intent ; l'écran « Payer maintenant » a conduit sur
  `/reservation?booking=…`).
- **E** — `/mes-reservations/avis/97369677-c5b7-4d77-bcab-0be79111c885` →
  « Vous avez déjà publié un avis » + « Avis publié » + note **9.0/10** ;
  `/mes-reservations` ne propose plus « Laisser mon avis » pour cette résa.
- **D** — `PATCH /api/users/me {"language":"en"}` → `user.language: en` ;
  `/` rendu avec `<html lang="en"` ; sans cookie → `<html lang="fr"` ;
  `<option value="en">EN</option>` présent dans le header.
- **C** — `/dashboard/analytics` (admin) → « Revenus par jour **(EUR)** »,
  `1 121,79 €`, `603,48 €` ; `/dashboard/billing` → `953,52 €` (net) ;
  hôte → « Revenus par jour (EUR) ».
- **B** — chambre USD `52e45fd8-a777-45d9-b1ef-fd652fd5ded0`
  (propriété `ef9d92f5-d815-42f5-a347-e3c6d43335a2`) : la fiche renvoie
  `currency: USD` ; POST `api/bookings` → `currency: "USD"`,
  `total: 187.00` ; `/mes-reservations` → **« 187,00 $US »** (aucun `€`).
- **G** — `npm run smoke` sur DB sans wishlist seed → création auto
  (`POST /api/wishlists`) puis 94/94.

## 3. Points vérifiés sans régression

- **Contrats API** : `register` (200), login, `GET /api/bookings` (contrat
  existant intact — `review` est *ajouté*, jamais retiré), `POST
  /api/bookings/[id]/payment` (200/409/403 inchangés), annulation
  `pending` (autorisée en plus de `confirmed`).
- **Défauts** : langue `fr` (compte sans `language`, anonyme → `<html
  lang="fr">`, repli `ar` → fr), devise EUR (cas réel seed → rendus
  identiques : `formatCurrencyBreakdown` mono-devise = `formatPrice`
  historique), CTA avis avant dépôt.
- **Base de données** : aucune migration, aucun fichier `drizzle` modifié ;
  18 tables inchangées (R14 ✓).
- **Cleanup pré-commit (vérifié par SELECT directs)** : aucun booking
  `MBB-2026-CMDMD9/TO7X38/GA6L7A`, aucun `pi_mock%` en `payment_intent_id`
  ni en outbox, aucun avis orphelin, wishlist_items/price_alerts supprimés,
  `users.language = fr` restauré. État : users 8 · bookings 32 · reviews 23 ·
  outbox 6 · wishlists 1 · wishlist_items 0 · price_alerts 0.

## 4. Limites restantes (honnêtes)

- **Playwright indisponible ici** (CDN Chrome for Testing bloqué) : les E2E
  `tests/e2e` restent CI-only ; les preuves ont été faites par HTML SSR +
  API HTTP + tests unitaires/intégration.
- **`€` codés en dur ailleurs** (hors périmètre B, qui couvre le tunnel de
  réservation) : `mon-compte` (wallet), `dashboard/properties/[id]` (prix
  /nuit), `bestrewards-status`, `promo-code-input`, `promotion-form` —
  à migrer vers `formatPrice(…, devise)` dans une future tâche.
- **i18n partielle** (D = sélecteur + `lang`, pas la migration complète) :
  93 composants restent en français dur ; le sélecteur n'agit que sur la
  langue résolue des composants déjà traduits. La migration incrémentale
  (pages publiques → messages/dashboard) est tracée dans
  `opportunites_T-152_2026-08-30.md`.
- **Analytics** : le % d'évolution courant/précédent est un rapport de flux
  toutes devises mêlées (étiqueté comme tel dans le code) ; seuls les
  montants affichés sont ventilés par devise. La série graphique est
  mono-devise (dominante) ; les autres devises sont signalées, pas mélangées.
- **Wallet** : crédit produit libellé EUR — jamais additionné aux montants
  de chambre (déduction affichée séparément).
